import React from 'react';
import ReactDOM from 'react-dom/client';
import PerchApp from './app';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <PerchApp />
  </React.StrictMode>
);